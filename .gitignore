node_modules/
dist/
build/
.env
.env.*
*.local
.DS_Store
*.sqlite
*.sqlite-*
.claude/settings.local.json

# Human-Input Log — local-only (Owner's choice, ADR-011): never pushed to the public repo.
human-input/
